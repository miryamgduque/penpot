{
  "name": "@penpot/skills-core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Penpot Skills — shared core: skill format parser, cascade resolver, builtin scope sources and the write-path guard. Consumed as TypeScript source by the Skills plugin (/ai-skills) and the MCP server/plugin (/mcp).",
  "main": "src/index.ts",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "tsx --test src/*.test.ts",
    "types:check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2"
  }
}
